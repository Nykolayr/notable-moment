import 'package:flutter/material.dart';
import 'package:notable_moments/features/questions/model/order_question.dart';

class OrderTestWidget extends StatelessWidget {
  final OrderQuestion question;
  const OrderTestWidget({super.key, required this.question});
  @override
  Widget build(BuildContext context) => const Placeholder();
}
